#include "ShadowRenderProvider.h"

namespace Eternity
{
	namespace Provider
	{
		CShadowRenderProvider::~CShadowRenderProvider()
		{
		}

		void CShadowRenderProvider::Register(CMeshRenderer* pData)
		{
		}

		void CShadowRenderProvider::Draw()
		{
		}

		// 地形描画処理
		void CShadowRenderProvider::DrawTerrain()
		{
		}

		// 固定メッシュ描画処理
		void CShadowRenderProvider::DrawMesh()
		{
		}

		// スキンメッシュ描画処理
		void CShadowRenderProvider::DrawSkinMesh()
		{
		}

		// ビルボード描画処理
		void CShadowRenderProvider::DrawBillboard()
		{
		}
	}
}